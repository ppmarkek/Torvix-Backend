services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: torvix
      POSTGRES_USER: torvix
      POSTGRES_PASSWORD: torvix
    ports:
      - "5432:5432"
    volumes:
      - torvix_pg:/var/lib/postgresql/data
volumes:
  torvix_pg:
